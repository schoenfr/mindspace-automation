[project]
name = "automation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "croniter>=6.2.2",
    "python-dateutil>=2.9.0.post0",
    "recurrent>=0.4.1",
]
